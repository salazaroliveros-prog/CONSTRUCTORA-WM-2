# Hosting config — NO se usa. Solo para referencia si necesitas Firebase Hosting
# La configuración oficial está en firebase.json (reglas Firestore/Storage)
# y vercel.json (configuración de despliegue Vercel)