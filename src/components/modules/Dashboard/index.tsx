// This wrapper is no longer used — App.tsx imports from ./components/Dashboard directly.
// Keep as pass-through for backward compat if any direct imports exist.
export { default } from "../../Dashboard";

