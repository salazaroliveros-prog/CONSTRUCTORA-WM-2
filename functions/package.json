{
  "name": "constructora-wm-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for ERP Constructora WM — cascade delete, sync, stock generation, periodic validation",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && npx firebase emulators:start --only functions",
    "deploy": "npm run build && npx firebase deploy --only functions",
    "logs": "npx firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "private": true
}
